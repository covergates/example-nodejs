{
  "name": "mocha_instanbul",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --cwd=../ --reporter=lcov mocha",
    "upload": "dotenv -- cross-var covergates upload --type lcov ../coverage/lcov.info"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "covergates": "^0.1.7",
    "cross-var": "^1.1.0",
    "dotenv-cli": "^3.2.0",
    "mocha": "^8.1.3",
    "nyc": "^15.1.0"
  }
}